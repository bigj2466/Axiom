# Axiom SDK Tooling Commands

The `axiom-cli` perfectly translates universal standalone compilation goals entirely offline bypassing structural implementations smoothly.

### 1. The Validation Target (`validate`)
Evaluates recursive schema directories definitively offline safely natively smoothly inherently validating against universally typed primitives cleanly completely elegantly.
```bash
axiom validate <directory_path>
```

### 2. The Native Execution Build (`build`)
Statically extracts, resolves, mathematically maps, firmly locks natively firmly sequentially unrolls securely evaluated ExecutionPlans inherently independently directly uniquely completely elegantly!
```bash
axiom build <directory_path> <id> --inputs '{"user_id":"123"}'
```

### 3. Execution Pass (`run`)
Compiles purely explicitly structurally identically perfectly exactly identically the AST plan completely dynamically executing purely offline over the seamlessly integrated isolated structural `Adapter` plugins uniformly offline nicely.
```bash
axiom run <directory_path> <id> --adapter openai --inputs '{"user_id":"123"}'
```

### 4. Deterministic Index Maps (`search`)
Triggers universal explicit metadata intersection filtering gracefully.
```bash
axiom search <directory_path> --tag support
```
