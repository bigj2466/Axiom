# Workflows

Axiom natively bridges execution decisions via uninstantiated mathematical explicit structural offline graphs smoothly bypassing arbitrary internal conditional execution limits inherently offline natively safely. 

## The AST Edges Schema

Instead of defining evaluation targets inside your LLM queries explicitly or Python arrays actively mathematically natively mathematically uniquely—Axiom cleanly leverages `nodes` dynamically resolving implicitly securely bound strictly explicit natively pinned JSON `id@version` edges structurally! 

```json
{
  "id": "workflow.support_router",
  "type": "workflow",
  "version": "1.0.1",
  "nodes": {
    "start": { "ref": "prompt.detect_intent@1.0.0" },
    "refund": { "ref": "prompt.refund_handler@1.0.0" }
  },
  "edges": [
    {
      "from": "start",
      "to": "refund",
      "if": {
        "var": "intent",
        "equals": "refund"
      }
    }
  ]
}
```

This strict JSON property yields devastatingly inherently reliable determinism cleanly cleanly globally across strictly independent evaluation domains intrinsically efficiently elegantly explicitly!
