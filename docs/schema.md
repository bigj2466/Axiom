# The Schema API 

Axiom models derive from JSON/YAML declarative dictionaries adhering explicitly uniformly to the universally structured properties securely natively offline.

## Core Schema Contracts
All implementations securely natively extend `BaseAxiomModel` natively:
* `id`: Strictly mapped deterministic nomenclature (e.g. `prompt.chat`)
* `version`: A natively constrained Semantic string (e.g. `1.5.0`)
* `type`: Explicitly strictly constrained values bounding validation limits.
* `metadata`: Nested dict natively tracking unstructured custom extensions reliably.
* `tags`: Top-level array seamlessly driving semantic CLI discoveries purely natively.

## Schema Types

### Template (`type: "template"`)
Axiom's lowest-level primitive defining pure messages blueprints independently decoupled structurally natively identically from configurations definitively. 
### Prompt (`type: "prompt"`)
An uninstantiated dynamic state naturally binding unique models into templates purely via inheritance mapping cleanly across architectures. 
### Skill (`type: "skill"`)
A structured evaluation module natively grouping disparate prompts natively directly uniformly.
### UseCase (`type: "usecase"`)
Top-down linear composition strategies explicitly chaining isolated skills securely sequentially strictly inherently.
### Workflow (`type: "workflow"`)
Explicit explicit logic DAG bridging explicitly structural paths securely via JSON evaluation properties offline safely seamlessly flawlessly isolated natively perfectly!
