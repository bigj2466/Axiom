# Axiom vs ContextFlow

Axiom and ContextFlow solve fundamentally different components of the enterprise AI orchestration puzzle. Mixing their unique responsibilities inherently destabilizes robust system deployments exactly fundamentally natively. 

### What is ContextFlow?
ContextFlow is an **Execution Engine**. It is structurally responsible for:
- Actively shuttling active memory.
- Actively establishing session limits.
- Validating explicit cache lifecycles natively offline dynamically.

### What is Axiom?
Axiom is a **Static Compiler**. It is strictly explicitly structurally responsible for:
- Locking explicit semantic versions cleanly across isolated dependencies.
- Translating linear skills explicitly or conditional declarative JSON offline branches into immutable JSON logic graphs.
- Never directly triggering HTTP requests natively or modifying network evaluations natively strictly enforcing determinism natively globally. 

**Rule of Thumb**: Axiom mathematically calculates *what* is executed universally. ContextFlow is practically responsible natively exactly for *how* it's physically transferred safely. You would pass an **Axiom ExecutionPlan** universally into **ContextFlow pipelines** exactly natively offline seamlessly.
